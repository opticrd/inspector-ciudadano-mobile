<?xml version="1.0" encoding="utf-8" ?>
<Grid
    x:Class="Inspector.Views.ProfileView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="http://prismlibrary.com">

    <Grid.Resources>
        <ResourceDictionary>

            <Style x:Key="TitleLabel" TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource PrimaryColor}" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <Style x:Key="ItemLabel" TargetType="Label">
                <Setter Property="TextColor" Value="#503026" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="HeightRequest" Value="32" />
                <Setter Property="VerticalTextAlignment" Value="Center" />
            </Style>

        </ResourceDictionary>
    </Grid.Resources>

    <ScrollView>
        <StackLayout Padding="16" Spacing="16">

            <Frame>
                <StackLayout Orientation="Horizontal">
                    <Image Source="icon.png" />

                    <StackLayout Spacing="8">
                        <Label Style="{StaticResource TitleLabel}">
                            <Label.Text>
                                <MultiBinding StringFormat="{}{0} {1}">
                                    <Binding Path="UserAccount.FirstName" />
                                    <Binding Path="UserAccount.LastName" />
                                </MultiBinding>
                            </Label.Text>
                        </Label>
                        <Label Text="{Binding UserAccount.Email}" />
                    </StackLayout>

                </StackLayout>
            </Frame>

            <!--<Frame>
                <StackLayout Spacing="8">
                    <Label Style="{StaticResource TitleLabel}" Text="Mi Dirección" />
                    <Label Text="Luis F. Thomen #419 Santo Domingo D.N." />
                </StackLayout>
            </Frame>

            <Frame>
                <StackLayout Spacing="8">
                    <Label Style="{StaticResource TitleLabel}" Text="Cuenta" />

                    <Label Style="{StaticResource ItemLabel}" Text="Editar perfil">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{prism:NavigateTo 'EditProfilePage'}" />
                        </Label.GestureRecognizers>
                    </Label>
                    <Label Style="{StaticResource ItemLabel}" Text="Notificaciones" />
                </StackLayout>
            </Frame>-->

            <Frame>
                <StackLayout Spacing="8">
                    <Label Style="{StaticResource TitleLabel}" Text="Otros" />

                    <Label Style="{StaticResource ItemLabel}" Text="Ayuda" />
                    <Label Style="{StaticResource ItemLabel}" Text="Términos y condiciones" />
                    <Label Style="{StaticResource ItemLabel}" Text="Politicas y privacidad" />
                    <Label Style="{StaticResource ItemLabel}" Text="Términos y condiciones" />
                    <Label Style="{StaticResource ItemLabel}" Text="Sobre el app" />
                    <Label Style="{StaticResource ItemLabel}" Text="Cerrar sesión">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding LogoutCommand}" />
                        </Label.GestureRecognizers>
                    </Label>
                </StackLayout>
            </Frame>

        </StackLayout>
    </ScrollView>

</Grid>